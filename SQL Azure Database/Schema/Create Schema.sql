-- ================================================
-- Create Schema Template for SQL Azure Database
-- ================================================

CREATE SCHEMA <sample_schema, sysname, sample_schema>
AUTHORIZATION <owner_name, sysname, owner_name>
